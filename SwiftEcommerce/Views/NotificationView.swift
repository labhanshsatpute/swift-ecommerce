import SwiftUI

struct NotificationView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Header(title: "Notifications")
                Spacer()
                VStack {
                    
                }
            }.padding(15)
        }
    }
}

#Preview {
    NotificationView()
}
